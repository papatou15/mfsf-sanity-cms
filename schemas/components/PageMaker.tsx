import { Box } from "@sanity/ui";
import React, {useState, useEffect} from "react";

interface PageMakerProps {
    pageTitle?: string
}

const PageMaker: React.FC<PageMakerProps> = ({pageTitle}) => {
    return(
        <Box>
            
        </Box>
    )
}

export default PageMaker